# Exported from Render on 2025-05-27T07:06:52Z
services:
- type: web
  name: Next-Bus-Backend Main
  runtime: python
  repo: https://github.com/Sourish-Kanna/Next-Bus-Backend
  plan: free
  envVars:
  - key: FIREBASE_CREDENTIALS_JSON
    sync: false
  region: oregon
  buildCommand: pip install -r requirements.txt
  startCommand: fastapi run main.py --host 0.0.0.0 --port $PORT
  autoDeployTrigger: commit
  
- type: web
  name: Next-Bus-Backend Dev
  runtime: python
  repo: https://github.com/Sourish-Kanna/Next-Bus-Backend
  branch: dev
  plan: free
  envVars:
  - key: FIREBASE_CREDENTIALS_JSON
    sync: false
  region: oregon
  buildCommand: 'pip install -r requirements.txt '
  startCommand: fastapi run main.py --host 0.0.0.0 --port $PORT
  autoDeployTrigger: commit
version: "1"
